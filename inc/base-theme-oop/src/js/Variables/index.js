import {
	protocol,
	host,
	domain,
	ext,
	directory,
	fullURL
} from './url'


const urlVars = {
	protocol: protocol,
	host: host,
	domain: domain,
	extension: ext,
	directory: directory,
	fullUrl: fullURL
}

export { urlVars }
